import React, { useState, useCallback } from "react";
import "./Playlist.css";

const Playlist = (props) => {

return (
  <div>
    <button>
    Save To Spotify
    </button>
  </div>
)
}
export default Playlist;